/**
 * Redux Basic Setup
 * -----------------------------------------
 * 1. Make store using `configureStore`
 * 2. make a connection with UI (must be client side)
 *      using <Provider store={store}></Provider>
 * 3. Create slice using `createSlice`
 *      - make actions on `reducers`
 *      - export all actions and slice.reducer
 * 4. Use action with `useDispatch` to handle storing the state by redux
 */

/**
 * Middleware Setup
 * -----------------------------------------
 * 1. 
 */